// @todo setup dotenv to load the following constants from an environment file (.env)

module.exports = config = {
    API_VERSION: 'v1',
    DEFAULT_CACHE_TTL: 30,
    MIDDLEWARE_CACHE_TTL: 60,
    DEBUG: false,
};
